// Optional: fill these in only when you want real cloud multi-user sync.
// With placeholders, the app runs in local demo mode with browser-saved accounts.
export const SUPABASE_URL = "YOUR_SUPABASE_PROJECT_URL";
export const SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY";
